`define COUNTER_WIDTH 6 // macros

module (clk, reset, direction, set, set_value, out);

parameter WIDTH = `COUNTER_WIDTH;

input clk, reset, direction, set;
input [WIDTH-1:0] set_value;
output reg [WIDTH-1:0] out;

always 